# `bnrpe`

Public API for the BNR-PE JAX reference package.

## Exported Symbols

- `BNRPEParams` (`class`, from `bnrpe.params`)
- `init_params` (`function`, from `bnrpe.params`)
- `apply_bnrpe` (`function`, from `bnrpe.rotors`)
- `apply_bnrpe_batch` (`function`, from `bnrpe.rotors`)
- `attention_logits` (`function`, from `bnrpe.rotors`)
- `generator_lowrank` (`function`, from `bnrpe.rotors`)
- `commutator_budget_penalty` (`function`, from `bnrpe.regularizers`)
